# 实时事实接口重构

## 范围

这是独立的 `hearthstone_companion` 插件，不修改 N.E.K.O 宿主。保留现有日志解析、公开信息过滤、不可变快照、配置与生命周期行为，不新增模型调用或对话拦截。

## 数据与入口

- `CompanionMonitor` 是状态唯一写入者；查询和事件读取同源公开快照。
- 被动 `read` 只提供一条有界概况：模式、轮次、阶段及必要的查询提示。不传逐卡数据，不要求模型拼接多个分段。
- `hearthstone_current_turn` 保留无参数同轮查询。
- `hearthstone_live_state` 提供明确的 `focus` 视图选择；可选自然语言问题只辅助选择，不能覆盖显式选择。
- Agent 入口保留官方目标检查和 `finish` 交付语义，复用同轮工具的事实输出，不维护第二套答案序列化器。
- Agent 受宿主独立短文本预算约束；实时查询短结果与工具一致，长结果明确省略详情并指向同轮工具，不宣称两种投递通道容量等价。赛季规则、英雄统计和赛后主题保留各自的数据投影，不能替换成当前回合摘要；主题内容超限时明确省略，不引导到不支持该主题的工具。
- 详细信息没有查询就不视为已送达；被动概况不能证明商店、手牌或场面已进入模型上下文。

## 不变量

未知不是零，未观测不是空场；公共目录只能补规则，不覆盖实时费用或关键词。断流、换源、旁观和撤销共享继续使实时查询失效。输出预算不足时明确标记省略，不宣称完整。

事实可读和建议可用分别判断。目录或经济证据不足可以阻止建议，但不能抹掉已经观测到的公开商店；缺失费用保持未知。诊断接受未知轮次的 null，具体检查点仍必须匹配其预期轮次。玩家首页在活动对局数据明确过期时提示暂不可查询，不把旧轮次标成当前状态。

开始、恢复、结束是固有生命周期；免打扰默认关闭，开启只抑制中局主动聊天。工具结果只经原 callback 返回，不另发主动消息补答。保留官方 SDK 注册与有界恢复，SDK 提交回执不等于角色已回复。

## 验收

1. 同一快照、同一视图只生成一份事实文本；Agent 超限时明确省略，不发送看似完整的截断场面。
2. 显式视图优先于自然语言推断；空问题仍能取得约定默认视图。
3. 被动消息为单条完整概况，保持预算、隐私、目标与失效覆盖约束。
4. 真实日志检查点继续证明构筑与酒馆详细事实；诊断分别记录概况送达、工具返回和模型回答。
5. 历史发行证据不改写，新源码不得冒用旧版本通过记录。

## 后续边界

新鲜度目前沿用既有策略。区分玩家思考期间状态不变与来源失联，需要单独的真实日志证据，不能以延长超时替代判断。剩余格式化层按被复用关系逐步收敛，不为减少文件行数拆出无职责边界的壳模块。
