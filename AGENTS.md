# AGENTS.md

本文件只约定代理如何在本仓库中协作。它不是用户教程、开发手册或发布清单；对应信息应以 `README.md`、`docs/`、`.github/workflows/` 和 N.E.K.O Plugin SDK 文档为准。

## 共同目标

- 主代理对最终结果负责，包括理解用户最新意图、确定范围、整合修改、验证结论和对外交付。
- 子代理负责一个边界清晰、可以独立验收的任务，不对整个项目作未经授权的决策。
- 所有代理以当前共享工作树为事实来源。聊天记录、旧测试结果和其他代理的摘要只能作为线索，不能替代对当前文件和状态的核对。
- 先解决用户真正遇到的问题，再考虑抽象、重构或扩展。不要为了表现工作量而扩大改动。

## 仓库上下文

所有参与任务的代理在行动前应知道以下不变量：

- 这是独立的 N.E.K.O 炉石陪玩插件仓库，不是 N.E.K.O 主项目中的内置插件。
- 产品核心是由公开局势驱动当前 N.E.K.O 角色自然陪伴；局势数据和酒馆建议是 LLM 的上下文能力，不应演变成本地规则短评工具。
- 原始 `Power.log`、玩家身份、隐藏牌和本机绝对路径属于敏感信息。不得把它们写入 LLM 上下文、网络请求、Store、测试夹具、提交或审查报告。
- 没有可靠来源时，不得声称提供全服胜率、排名或 meta 结论。
- 未经用户明确授权，不得推送、合并 `main`、创建或删除 tag/Release、改写远端历史、投稿市场或执行其他外部发布动作。
- 不为未发布且无人使用的旧版本主动增加兼容层，除非用户明确要求。

## 主代理职责

主代理必须持续维护全局视图，而不是把任务拆完后等待结果：

1. 开始时确认用户最新请求、当前分支、工作树状态和相关文档。
2. 判断任务是回答、诊断、实现、审查还是发布；不得把只读请求扩展为写入或外部操作。
3. 划分必须由自己串行处理的主链路，以及适合独立并行的子任务。
4. 给每个子代理明确任务契约，并避免多个代理同时修改同一组文件。
5. 在子代理工作期间继续处理可独立推进的主任务，及时回应新用户消息。
6. 验收子代理结论，亲自阅读关键代码和必要的项目/SDK 约束，不盲目采纳摘要。
7. 汇总改动后运行与风险相称的最终验证，检查临时文件、未跟踪文件和进程是否清理。
8. 最终只陈述有证据支持的结果，明确区分“通过”“未运行”“环境不具备”和“仍有风险”。

主代理不能把最终集成、权限判断、提交范围或交付结论委托出去。

## 何时使用子代理

运行环境允许且存在实际并行收益时，优先考虑子代理。适合的任务包括：

- 独立阅读 N.E.K.O SDK、官方插件或外部项目并提交有来源的调研结论。
- 对生命周期、隐私、日志解析、酒馆状态、Hosted UI、打包发布等不同风险域进行独立审查。
- 为一个明确模块实现修复，并拥有互不重叠的文件写入范围。
- 运行隔离的故障注入、兼容性测试、打包检查或安装验证。
- 在实现完成后进行只读交叉审查，寻找实现者容易遗漏的竞态和失败路径。

以下情况不应拆给子代理：

- 几分钟即可完成、且结果是下一步唯一输入的串行操作。
- 需要持续修改同一个核心文件、会产生高冲突成本的工作。
- 目标、授权或验收标准尚不清楚的探索。
- 只是为了重复主代理已经完成的普通检查。

子代理数量服从任务结构，不追求占满并发槽位。高风险任务通常更适合“一个实现者 + 一个或两个独立只读审查者”，而不是多人同时编辑。

## 子代理任务契约

派发任务时至少说明：

- **目标**：需要回答的问题或需要达到的行为。
- **范围**：允许阅读和修改的模块、文件或系统。
- **权限**：只读还是可写；是否允许运行网络、构建或安装操作。
- **约束**：隐私边界、共享工作树现状、禁止执行的操作。
- **验收**：应运行的定向测试或应提供的证据。
- **交付**：findings、修改文件、测试结果、遗留风险和阻塞项。

推荐的派发格式：

```text
目标：审查日志源切换期间是否可能向 LLM 暴露旧局势。
范围：只读检查 monitor.py、__init__.py 及对应测试。
约束：不要修改文件，不要提交；共享工作树可能继续变化。
验收：给出可复现路径、严重级别和精确文件位置；无发现也要说明检查过的边界。
交付：findings 优先，其次是测试建议和残余风险。
```

目标改变时，主代理应给现有代理发送补充说明；旧任务已经失去价值时应及时停止，不让代理在过时目标上继续消耗时间。

## 共享工作树规则

所有代理共享同一个文件系统，必须按并发环境工作：

- 每个代理开始前检查当前状态；任何不认识的改动默认属于用户或其他代理。
- 不回退、不覆盖、不清理其他人的修改。遇到重叠时先停止写入并通知主代理。
- 写任务尽量按文件或模块分配唯一所有者。只读审查可以并行，重叠写入不可以。
- 修改前重新读取目标文件；长时间调研后尤其不能依据旧内容直接打补丁。
- 子代理不得自行提交、rebase、merge、push、tag 或发布，除非任务契约明确授权。
- 主代理暂存时使用显式文件列表，先检查 diff，避免把缓存、日志、构建产物或其他代理的临时文件带入提交。
- 临时目录使用可识别的任务专用名称；创建者负责在任务结束前确认并清理，且不得删除预先存在的目录。
- 长时间运行的命令、服务和后台会话由启动者负责等待、停止或明确交接。

如果共享工作树在审查期间变化，审查者必须说明结论对应的 commit 或工作树状态；不能把旧快照的结果当作当前结论。

## 审查与修复闭环

审查的目标是发现真实风险，不是总结实现。默认按以下顺序报告：

1. 可导致隐私泄漏、数据破坏、崩溃或错误外部操作的问题。
2. 核心功能失效、竞态、状态错配和无法恢复的问题。
3. 边界行为、性能、可维护性和测试缺口。

每条 finding 应包含严重级别、触发条件、实际影响、文件位置和建议验证方式。没有发现时，说明检查了哪些边界以及仍无法覆盖的风险。

修复闭环由主代理驱动：

1. 复现或证明 finding，而不是仅凭猜测改代码。
2. 添加能在旧实现失败的回归测试。
3. 实施范围最小且符合现有架构的修复。
4. 运行定向测试，再让独立审查者检查相邻失败路径。
5. 合并所有修改后运行最终全量门禁。
6. 若出现新 finding，重复以上过程，直到没有可操作问题或明确记录无法消除的阻塞。

并发、配置事务、同意权限、日志切源、启动停止和异步写入属于高风险区域。修改这些区域时，应主动安排独立的竞态/隐私审查，而不只依赖功能测试。

## 验证分工

- 实现代理负责与其修改直接相关的快速测试，并报告完整命令和结果。
- 审查代理可以增加故障注入或定向测试，但不能以“测试通过”替代代码推理。
- 主代理负责最终全量测试、静态检查、SDK/Hosted 兼容性和提交前状态检查。
- 多个代理不要无意义地同时运行相同的重型全量任务；优先分配不同验证面，最后由主代理统一复验。
- 测试证据必须对应当前代码。任何后续源码修改都会使之前的最终验证失效，至少需要重新运行受影响检查。
- 真实日志验证只能输出脱敏摘要；缺少真实日志时必须如实说明，不能用夹具测试冒充实机回放。

精确的质量门禁和发布依赖以 `.github/workflows/` 为准；SDK 行为以仓库固定版本和官方文档为准，不在本文件复制容易过期的命令清单。

## 子代理回报格式

子代理结束时应提供简洁、可验收的回报：

```text
结论：完成 / 有 finding / 阻塞
修改：文件列表，或“只读，未修改”
验证：执行内容与结果
发现：按严重级别列出，附文件位置
残余风险：未覆盖的环境、数据或场景
临时资源：已清理，或明确列出需主代理接管的路径/会话
```

不要只回复“已完成”或粘贴大段日志。主代理需要能据此判断是否采纳、复查或继续迭代。

## 最终交付检查

主代理在结束任务前确认：

- 用户最新请求已全部处理，没有沿用被后续消息取代的旧目标。
- 所有子代理都已回报、停止或明确交接，没有遗留运行会话。
- 关键 findings 已修复并复审；未修复项有清楚的影响和原因。
- 工作树只包含预期文件，临时资产和敏感数据未进入提交。
- 测试、构建、安装或实机结论均有对应证据，未执行的部分已明确说明。
- Git、GitHub、Release 和市场状态描述准确，没有把本地提交说成已推送或已发布。

相关资料入口：

- 用户使用与能力：`README.md`、`docs/quickstart.md`
- 系统设计：`docs/architecture.md`
- 数据边界：`docs/privacy-security.md`
- 市场说明：`docs/marketplace.md`
- CI 与发布门禁：`.github/workflows/`
